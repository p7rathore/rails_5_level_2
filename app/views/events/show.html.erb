<article>
	<header>
		<h1><%= @event.name %></h1>
	</header>
	<p>
		<%= @event.description%>
	</p>
	<h3>When:</h3>
	<P>
		<%= @event.starts_at %>
			
	</P>
	<h3>Where:</h3>
	<P>
		<%= @event.location %>
			
	</P>

	<h3>Price:</h3>
	<P>
		<%= format_price(@event) %>
	</P>

</article>

<%= link_to "All Events", events_path %> |   
<!--<span>&#124;</span> it's show vaertical line b/w both button !-->
<%= link_to "Edit", edit_event_path(@event) %>