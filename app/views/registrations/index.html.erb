<h1><%=pluralize(@registrations.size, 'Registration')%> for <%= link_to @event.name , @event %></h1>

<ul>
	<%@registrations.each do |r| %>
		<li><%=link_to r.user.name, r.user %> |
		  <%= time_ago_in_words(r.created_at) %> ago
		  (<%= r.how_heard%>)
		 </li>
	<% end %>
</ul>


